[DEFAULT]
source_dir = #directory all files are uploaded do
destination_dir = #directory all files are deposited to
titledb = #where the titledb lives, needs to exist
staging_url = #LOCKSS crawable URL
logfile = #creates on first run
weblog = #updates per AU ie: as each log entry is added
max_au_size = #maximum size in bytes, 5000000000 equates to 50gb

[DROID]
java_path = #path to java executable
droid_path = #path to DROID command line jar
droid_log = #path to DROID csv log